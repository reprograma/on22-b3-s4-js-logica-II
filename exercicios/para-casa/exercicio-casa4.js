// criar um array com 5 números
const numeros = [1, 2, 3, 4, 5];

// define uma variável de controle inicializada com zero
let i = 0;

// percorra o array com um loop while e imprima cada número no console
while (i < numeros.length) {
  console.log(numeros[i]);
  i++;
}
